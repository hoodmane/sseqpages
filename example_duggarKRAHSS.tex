\documentclass{article}
\usepackage[landscape]{geometry}
\usepackage{sseqpages}
\begin{document}
\makeatletter
\begin{sseqdata}[
    name=KRASS,
    x range={0}{16},
    y range={0}{10},
    x label step=2,
    classes={circle,inner sep=0.3ex,fill=black},
    differentials=->,
    execute at begin node=$,execute at end node=$,
    degree={-1}{#1-1},
    scale=0.5,
    x clip padding=0.1cm,
    y clip padding=0.05cm
]

\def\sseq@xlabelgap{0.5cm}
\def\sseq@ylabelgap{0.5cm}
\def\sseq@xaxis@end@extend{0.25cm}
\def\sseq@yaxis@end@extend{0.25cm}
\def\sseq@xclip@axisgap{0.05cm}
\def\sseq@yclip@axisgap{0.05cm}
\def\sseq@xclip@extend{0.1cm}
\def\sseq@yclip@extend{0.1cm}

\draw[background,xshift=-0.5cm,yshift=-0.5cm,step=1cm,gray,very thin] (\xmin,\ymin) grid (\xmax+0.9,\ymax+0.9);
\pgfmathparse{\xmax+2}
\foreach \x in {0,4,...,\pgfmathresult} {
    \class[fill=none,draw,inner sep=0.6ex](\x,0)
    \foreach \z in {0,...,\pgfmathresult} {
        \class(\x+\z+1,\z+1)
        \structline(\x+\z,\z)(\x+\z+1,\z+1)
    }
}

\foreach \x in {4,12}{
    \foreach\z in {0,...,\xmax}{
        \d4(\x+\z,\z)
    }
    \replaceclass[fill=none,rectangle,draw,inner sep=0.6ex](\x,0)
}
\end{sseqdata}
\printpage[name=KRASS,page=0]
\printpage[name=KRASS,page=5]

\end{document}